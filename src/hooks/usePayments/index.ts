// Este hook ya no es necesario porque el estado de pagos se gestiona de forma centralizada
// en el componente padre (FormContracts.tsx) usando useState.

// El estado de pagos ahora se pasa como props a los componentes hijos
// para garantizar una única fuente de verdad y actualizaciones en tiempo real.

export {};
